version: "3.9"
services:
  fixture-target:
    image: python:3.12-slim
    command: ["python", "-m", "uvicorn", "services.trace.fixture_target:app", "--host", "127.0.0.1", "--port", "8080"]
    volumes:
      - ./:/app
    working_dir: /app
    ports:
      - "127.0.0.1:8080:8080"
